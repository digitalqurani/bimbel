rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /pendaftaran/{docId} {
      allow create: if true; // anyone can add a registration
      allow read, update, delete: if request.auth != null && request.auth.token.email == "admin@mdiq.com";
    }
  }
}
